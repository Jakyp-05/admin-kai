import { styled } from "@mui/material";

export const Container = styled("div")(({ theme }) => ({
  maxWidth: "1440px",
  width: "100%",
  margin: "0 auto",
  display: "flex",
  flexDirection: "row",
}));
